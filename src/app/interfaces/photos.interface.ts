export interface Photos {
  id?: string;
  url: string;
}

export interface WebPhotos {
    id: string;
    author: string;
    width: number;
    height: number;
    url: string;
    download_url: string;
}
